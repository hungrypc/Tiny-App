<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include ./partials/_header %>
  <p></p>
  <h5>Index of Links</h5>
    <ul>

     <% for (shortUrl in urls) { %>
        <li><a href="http://localhost:8080/u/<%= shortUrl %>"><%= shortUrl %> </a>  -  <%= urls[shortUrl] %>
          <div>
            <form method="GET" action="/urls/<%= shortUrl %>" style="display: inline-block">
              <button type="submit" class="btn btn-primary">Edit</button>
            </form>
            <form method="POST" action="/urls/<%= shortUrl %>/delete" style="display: inline-block">
              <button type="submit" class="btn btn-primary">Delete</button>
            </form>
          </div>
        </li>
    <% }; %>

    </ul>

</body>
</html>